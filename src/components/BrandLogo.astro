---
import { Image } from 'astro:assets';
import logo from '@images/hiko-logo.png';

interface Props {
  class?: string;
}

const { class: className = 'h-auto w-24' } = Astro.props;
---

<Image src={logo} alt="Hiko Matcha" class={`object-contain ${className}`} />
